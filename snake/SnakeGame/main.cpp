//  SnakeGame — Classic Console Snake for Windows
//  Build with Visual Studio 2019 / 2022  (Console App, x64 or x86)
//  No external dependencies — Windows API + standard C++ only.

#include "Game.h"

int main()
{
    Game game;
    game.Run();
    return 0;
}
